# Design module (frontend)

Scaffold — components, stores, and API client for the Design module go here, mirroring
`lib/modules/diagnose/`. The boilerplate page at `routes/design/+page.svelte` currently renders
a placeholder using `lib/shared/components/ModuleHeader.svelte`; wire it up to real components
from this folder as the Design flow is built.
